# Per-user config (written by install.sh)
config.json
venv/
__pycache__/

# The unified content store is ~/.agent-home (NOT in this repo). This repo holds
# only the setup code. Nothing personal is ever committed here.
